# Nombre base del proyecto. El perfil activo sobreescribe este valor.
spring.application.name=api-gateway

# Perfil local por defecto. Docker Compose define el perfil prod.
spring.profiles.active=dev
